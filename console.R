source("E:\\applide science degree\\coursera\\r\\swirl\\Peer-graded-Assignment-Programming-Assignment-2-Lexical-Scoping\\makeCacheMatrix.R")

my_matrix <- makeCacheMatrix(matrix(1:4, 2, 2))


my_matrix$get()

my_matrix$getInverse()

cacheSolve(my_matrix)

cacheSolve(my_matrix)

my_matrix$getInverse()

my_matrix$set(matrix(c(2, 2, 1, 4), 2, 2))

my_matrix$get()

my_matrix$getInverse()

cacheSolve(my_matrix)

cacheSolve(my_matrix)

my_matrix$getInverse()